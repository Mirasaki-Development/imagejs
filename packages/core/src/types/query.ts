export enum QueryParam {
  format = 'f',
  width = 'w',
  height = 'h',
  quality = 'q',
}

export enum TransformQueryParams {
  progressive = 'pr',
  crop = 'c',
  trim = 't',
  gravity = 'g',
  background = 'bg',
}